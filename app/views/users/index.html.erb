<h1>Listing users</h1>

<table>

<tr>
 <td>UserID</td>
 <td>name</td>
</tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.user_id %></td>
    <td><%= user.name %></td>
    <td><%= link_to 'Show', user %>   <%= link_to 'Edit', edit_user_path(user) %>   <%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />


<%= link_to 'test_board' , test_boards_path %>
<%= link_to 'New User', new_user_path %>
