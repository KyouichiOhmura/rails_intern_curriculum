<%= form_for(@view_test) do |f| %>
  <% if @view_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@view_test.errors.count, "error") %> prohibited this view_test from being saved:</h2>

      <ul>
      <% @view_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
